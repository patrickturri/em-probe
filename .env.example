# Copy to .env (gitignored). Loaded by src/judge.py via python-dotenv.
# Judge model is Claude Haiku (see README limitations vs the papers' GPT-4o).
ANTHROPIC_API_KEY=sk-ant-...

# Optional: HuggingFace token if you hit anonymous rate limits.
# HF_TOKEN=hf_...
